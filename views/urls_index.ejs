<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <h2> Loop </h2>
  <ul>
    <%  for ( let key in urls ) { %>
      <li> <%= `${key}: ${urls[key]}` %></li>
    <% } %>
    </ul>
</body>
</html>